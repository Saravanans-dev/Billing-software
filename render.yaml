services:
  - type: static_site
    name: free-frontend
    repo: https://github.com/Saravanans-dev/Billing-software
    branch: main
    buildCommand: cd frontend && npm install --include=dev && npm run build
    publishPath: frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
